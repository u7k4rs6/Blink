# G5: CDP screencast relay at 12 tiles

**Question.** What frame rate and bandwidth does one Node process sustain relaying 12 concurrent CDP screencasts against our own previewUrl target (Q11)?

**Verdict.** 12/12 tiles streamed 4542 frames. Median 8.0 fps per tile, 355 KB/s aggregate (2.9 Mbps), 4 KB per frame. Within the section 7 prediction of about 720 KB/s.

**Observations.** 4542 screencast frames, against a declared minimum of 12.

Run `g5-2026-09-03T11-55-06-205Z` on plan `starter`, 2026-09-03T11:56:23.370Z.

## Aggregate

| measurement | predicted (section 7) | measured |
|---|---|---|
| tiles | 12 | 12 streamed, 0 failed |
| fps per tile | about 4 | p50 8.0, p95 8.4, min 6.6 |
| bytes per frame | about 15 KB | 4 KB |
| aggregate outbound | about 720 KB/s (5.8 Mbps) | 355 KB/s (2.9 Mbps) |
| total for a 45 s run | about 32 MB | 15.6 MB |

## Per tile

| tile | frames | KB | fps | first frame | error |
|---|---|---|---|---|---|
| 0 | 380 | 1337 | 7.9 | 3391 ms |  |
| 1 | 383 | 1346 | 8.0 | 3468 ms |  |
| 2 | 379 | 1334 | 7.9 | 3587 ms |  |
| 3 | 326 | 1146 | 6.8 | 3586 ms |  |
| 4 | 407 | 1432 | 8.4 | 3586 ms |  |
| 5 | 386 | 1356 | 8.0 | 3661 ms |  |
| 6 | 387 | 1360 | 8.0 | 3710 ms |  |
| 7 | 396 | 1392 | 8.2 | 3618 ms |  |
| 8 | 392 | 1375 | 8.1 | 3775 ms |  |
| 9 | 390 | 1370 | 8.0 | 3750 ms |  |
| 10 | 322 | 1133 | 6.6 | 3894 ms |  |
| 11 | 394 | 1385 | 8.1 | 3867 ms |  |

## The relay target, and two free readings from it

Every tile pointed at **our own sandbox**, a fresh base-template fork serving a trivial animated page on port 8099 behind `previewUrl`. No third-party site was loaded, which the non-goals forbid and which would be abusive at 12 concurrent browsers regardless. It is also the right target on the merits: it is the same shape as a real Swarm run, a Solari sandbox behind `previewUrl`, rather than someone else's CDN.

### previewUrl response headers (early partial read on G2)

| header | value |
|---|---|
| status | 200 |
| x-frame-options | absent |
| content-security-policy | absent |
| server | SimpleHTTP/0.6 Python/3.11.2 |
| set-cookie | present |
| access-control-allow-origin | absent |

This is one app on one port, so it is a partial read and not G2's answer. G2 still has to check three real apps, because a framing header set by the *app* (Gitea, Uptime Kuma) is a different question from one set by the *preview domain*. What this does settle is which headers the preview domain itself adds, since the page behind it is a bare `python3 -m http.server` that sets almost nothing: the preview domain adds **no** framing headers of its own, so whatever G2 finds on a real app comes from that app and is therefore configurable.

### Where the previewUrl overhead actually goes

| path | first request | steady-state p50 | steady-state p95 | min |
|---|---|---|---|---|
| server to previewUrl | 817 ms | 265 ms | 1429 ms | 251 ms |
| inside the guest, over loopback | 1 ms | 0 ms | 1 ms | 0 ms |

**The overhead is largely one-off routing setup.** The first request cost 817 ms and steady state settled to 265 ms. So the 1196 ms seen earlier was first-request cost, not a per-request tax, and fork-to-healthy does **not** carry a ~1.2 s floor. S2's headline stands as a fork-speed number.

**Preview-domain overhead is about 265 ms per request** (265 ms through previewUrl versus 0 ms over loopback inside the guest). The loopback figure is the app's own service time; everything above it is routing.

**Design consequence, and it holds either way: the health check runs over loopback inside the sandbox via `commands.run`, not through `previewUrl`.** The launch path polls health up to 15 s at 250 ms intervals, which is up to 60 requests. Paying preview-domain overhead on every one of them inflates fork-to-healthy by routing cost that a visitor never experiences as part of the fork. `previewUrl` is then resolved once, for the handover, and its first-byte cost is reported as its own number rather than hidden inside the fork time. Written into `02-architecture.md` sections 2.1 and 2.2.

### The Set-Cookie on the preview domain

```
AWSALB=REDACTED; Expires=Thu, 10 Sep 2026 11:55:15 GMT; Path=/
AWSALBCORS=REDACTED; Expires=Thu, 10 Sep 2026 11:55:15 GMT; Path=/; SameSite=None; Secure
__pt_preview=REDACTED; Domain=91975b313747a559a3e8-8099.preview.getsolari.com; Path=/; HttpOnly; Secure; SameSite=None; Partitioned; Max-Age=3597
```

Not obviously the preview token. Name and attributes are above; identify it before assuming it is harmless, since the origin server sets no cookies at all.

### Does previewUrl hold up under 12 concurrent clients

| clients | ok | failed | p50 | max |
|---|---|---|---|---|
| 12 | 12 | 0 | 1242 ms | 1315 ms |

All 12 concurrent requests succeeded, so `previewUrl` does not fall over at Swarm concurrency. The Swarm design assumes exactly this and nothing else in the harness tests it.

## What this means

The relay runs in one Node process alongside the orchestrator (02-architecture.md section 1), so this number is a budget for the whole server, not just the grid. On the chosen host, Hetzner CX22 with 20 TB of traffic (V52), 2.9 Mbps for 45 seconds per run is 15.6 MB, and the traffic allowance is not the constraint. CPU in one process is.

If measured fps is far below 4, the backpressure design in section 7 (acknowledge a frame only after the visitor socket drains, and double `everyNthFrame` for a tile more than 2 s behind) is doing the right thing but from a worse starting point, and the grid should ship at fewer tiles rather than a slower grid.

## Cost

| | sandbox-seconds | browser-seconds | USD at starter |
|---|---|---|---|
| Estimated before start | 240 | 780 | $0.02547 |
| Actual after finish | 76 | 858.1 | $0.02504 |

### Measured versus modelled

| | USD | share |
|---|---|---|
| measured from observed lifetimes | $0.02504 | 100% |
| modelled from flat estimates | $0.00000 | 0% |

Every row in this gate came from an observed lifetime. Nothing here is modelled.

| row | kind | seconds | USD | measured |
|---|---|---|---|---|
| 1 | browser | 858.1 | $0.02384 | yes, 12 browser sessions, observed elapsed |
| 2 | sandbox | 76 | $0.00120 | yes, relay target sandbox, observed lifetime |

Ceiling for this gate was $0.05000. Total gates budget is $0.40.

Attempts recorded: 5. Any attempt beyond the first for one logical call is a retry, and the gate caps retries at zero (`CAPS_GATES`).