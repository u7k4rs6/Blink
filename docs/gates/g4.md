# G4: snapshot limits on Starter

**Question.** What are the snapshot count, size, creation time and deletion rules on Starter (Q2), and does a snapshot survive downgrade (Q12)?

**Verdict.** Minted 5/5 snapshots with no ceiling reached. Creation time 10309 to 11354 ms. Deletion with a live child refused as documented. Cleaned up 5, left 0.

Run `g4-2026-09-03T11-20-05-074Z` on plan `starter`, 2026-09-03T11:21:18.766Z.

## Inventory

|  | count |
|---|---|
| snapshots before this gate | 6 |
| minted by this gate | 5 |
| snapshots after cleanup | 6 |

## Per snapshot

| # | creation ms | size bytes | size MiB |
|---|---|---|---|
| 1 | 10309 | 3839483980 | 3662 |
| 2 | 11229 | 3841449386 | 3663 |
| 3 | 11354 | 3841449386 | 3663 |
| 4 | 11082 | 3841449389 | 3663 |
| 5 | 11045 | 3841449387 | 3663 |

**No ceiling was reached at 5 snapshots.** That does not prove there is none, only that it is above 5. Raise `BLINK_G4_PROBES` to push further, at proportional cost.

## Deletion with a live child (V23)

deletion refused as documented: Snapshot has live children and cannot be deleted

D9's expiry sweeper must treat a 409 as a normal re-queue for the next tick, never as an error and never as something to retry in place. Instances live 20 minutes at most, so the conflict always clears on its own.

## Downgrade survival (Q12): the scheduled re-inventory

**Not tested here, and deliberately so.** Testing it by downgrading would cost the Starter month. But the downgrade is **scheduled, not hypothetical**, so Q12 has an answer date rather than an open question mark. **TODO: the Starter end date is not set.** Set `BLINK_STARTER_ENDS=YYYY-MM-DD`, or edit `STARTER_ENDS` in `src/guard/rates.ts`. Until it is set, Q12 stays unanswerable, because nobody will remember to look.

### Pre-downgrade inventory, captured by this run

| field | value |
|---|---|
| snapshots held | 6 |
| snapshot ids | snap_dl4yditus22a, snap_dl50p1pj5842, snap_dl4x5fwmhxbj, snap_dl50q1h9wk1b, snap_dl4x5frwj6ib, snap_dl50rvgrafrw |
| total bytes | 23712578982 |
| captured at | 2026-09-03T11:21:18.765Z |

Baseline written to `/home/utkuputku/Desktop/Blink/docs/gates/data/g4-baseline.json`.

### The exact step to run the day after the downgrade

One command. It reads the stored baseline, re-inventories snapshots, reads the current plan limits, diffs them, and writes `docs/gates/G4-q12-downgrade.md`.

```bash
# On <the day after BLINK_STARTER_ENDS>, with the account already on Free:
BLINK_PLAN=free npm run gates -- --q12
```

It **refuses to run** if `BLINK_STARTER_ENDS` is unset, if today is before that date, or if no baseline exists. Running early would record a non-answer that looks like an answer, and a non-answer in the file is worse than an empty file because the next reader believes it.

It answers three questions and writes them up:

1. **Did the stored snapshots survive?** If they are deleted on downgrade, D1 and D9 both break on the first day of Free and the canary cannot rebuild.
2. **Did their sizes change?** A silent re-encode or a size cap shows here.
3. **What happened to anything running at the moment of the drop?**

**One step has to happen before the date, not after.** Question 3 cannot be reconstructed retrospectively, so leave one cheap sandbox alive across the boundary the night before. Without that, question 3 stays permanently unanswered for this account, and `--q12` will say so rather than guess.



## Cost

| | sandbox-seconds | browser-seconds | USD at starter |
|---|---|---|---|
| Estimated before start | 330 | 0 | $0.00523 |
| Actual after finish | 330 | 0 | $0.00523 |

Ceiling for this gate was $0.03000. Total gates budget is $0.40.

Attempts recorded: 18. Any attempt beyond the first for one logical call is a retry, and the gate caps retries at zero (`CAPS_GATES`).