# G6: outbound egress restriction

> **Verdict re-derived 2026-09-03T15:33:53.152Z from corrected logic over the ORIGINAL stored data.**
>
> No Solari call was made and no new observation was taken. The bytes in
> `docs/gates/data/g6.json` from run `g6-2026-09-03T11-54-23-031Z` are unchanged; only the
> logic that reads them was fixed.
>
> The original run printed:
> 
> > INCONCLUSIVE. The gate did not reach the egress test, so this run is evidence of NOTHING about Q5, in either direction. Fix the blocking step and re-run before drawing any conclusion.
> 
> The corrected logic produces the verdict below. **This is code over data, not a
> human reading over a disagreeing report.**

**Question.** Can outbound network from the sandbox be restricted, by a guest-side nftables firewall or otherwise (Q5)?

**Verdict.** **Q5 RESOLVES NEGATIVE for the guest-side approach, on evidence.** The guest runs as root with every capability set (`CapEff: 000001ffffffffff`), yet the kernel has no netfilter subsystem: `nft add table` returns "Operation not supported", `nft list ruleset` fails cache initialization, no nf_tables module is loaded, and neither modprobe nor iptables exists. This is not a permission gap that could be granted; it is a missing kernel subsystem, which cannot be granted, loaded or worked around. No guest-side firewall is possible, so this is an answer rather than an open question, and no workaround will be attempted. **Blink ships with NO network restriction**, and 03-security-and-access.md records that as a known accepted gap.

**Observations.** 5 decided steps, against a declared minimum of 5.

## Steps

| step | result | detail |
|---|---|---|
| probe tool (curl) available | pass | present in the base template |
| snapshot size delta for a real install | pass | before 3839483309 B, after 3841514857 B, delta 2031548 B. Payload: gitea version 1.27.3 built with go1.26.7-X:jsonv2 : bindata
126007248
/dev/root      ext4     |
| baseline outbound works | pass | curl returned "200" |
| nftables available in guest | pass | /usr/sbin/nft
INSTALLED |
| deny-all-except-loopback installed | FAIL | Error: Could not process rule: Operation not supported
add table inet blink
^^^^^^^^^^^^^^^^^^^^^
APPLY_FAILED |
| why the firewall could not be installed | n/a | uid=0 \| CapEff: CapEff:	000001ffffffffff \| nf_tables module: 0 \| 0 \| modprobe: sh: 1: modprobe: not found \| nft list: netlink: Error: cache initialization  |

## Snapshot size for a real install (Q18)

|  | bytes | GiB |
|---|---|---|
| bare base template | 3839483309 | 3.58 |
| after installing the Gitea binary | 3841514857 | 3.58 |
| delta | 2031548 | 0.002 |

**Anomaly, and it is not resolved, but one explanation is now ruled out.** A 126 MB binary was written to `/opt` and the reported snapshot size moved only 2031548 bytes. The tmpfs theory is dead: `df -PT` shows `/opt` and `/tmp` are both `/dev/root ext4`, a real disk, so the bytes did land on the filesystem. So **`SnapshotView.sizeBytes` is not a content measure you can plan against**: it is either an incremental figure against the parent, or it is computed lazily and was read too soon after `snapshot()` returned. The lazy-computation theory is the one to test next, by re-reading `listSnapshots()` after a delay. The ~3.84 GB floor for a bare template is real and repeatable; what one app *adds* is still unmeasured. Until that is settled, D9's cap of 12 live share snapshots is justified by count rather than by bytes, and the "roughly 46 GB" figure in `02-architecture.md` section 2.8 is an upper-bound guess, not a measurement. Q18 needs the written answer either way.

## Filesystem layout (settles the /tmp tmpfs question)

```
126007248
/dev/root      ext4     4046560 1699744   2120720      45% /
/dev/root      ext4     4046560 1699744   2120720      45% /
```

The last two lines are `df -PT` for `/opt` and `/tmp`. If `/tmp` shows type `tmpfs` it is RAM-backed, which is why the earlier 126 MB write to `/tmp` moved the reported snapshot size by 677 bytes: those bytes never reached the disk image. This run wrote to `/opt` instead, so the delta above is measured against a real filesystem.

`## Why the firewall could not be installed`

```
uid=0
CapEff: CapEff:	000001ffffffffff
nf_tables module: 0
0
modprobe: sh: 1: modprobe: not found
nft list: netlink: Error: cache initialization failed: Invalid argument
iptables fallback: sh: 1: iptables: not found
```

**This is a missing kernel subsystem, not a permission problem, and that is the stronger negative.** The guest is root with every capability set. There is simply no netfilter in this kernel to configure. A permission gap could in principle be granted; this cannot be. Q5 is answered negative for the guest-side approach and no workaround is appropriate.

## What this decides

**T1 (mining), T2 (outbound proxying) and T3 (malware hosting)** in `03-security-and-access.md` all name egress denial as their primary control. No platform feature for it is documented anywhere (00-verification.md V32), so a guest-side firewall is the only candidate, and this gate is the only evidence either way.

**Blink ships with no network restriction, and that is a known accepted gap.** The guest cannot hold CAP_NET_ADMIN, so no guest-side firewall exists to install, and Solari documents no platform-level egress control (V32). The whole control for T1 and T2 is therefore: 1 vCPU being a poor miner, memory caps, short lifetimes, the metrics() CPU sampler, and layer 2 of the three-layer expiry, which is the only layer indifferent to visitor activity. `03-security-and-access.md` states this plainly rather than implying a restriction exists.

**The v1.5 pty terminal.** Undetermined, so the terminal ships under the stricter of the two options.