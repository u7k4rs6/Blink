# G3: pause billing, resume latency, and Q17

**Question.** Does a paused sandbox free its slot, how long does resume take (Q7), and does previewUrl traffic reset the idle clock (Q17)?

**Verdict.** Resume to healthy p50 4740 ms, p95 5904 ms. Pause frees the concurrency slot: CONFIRMED. PREVIEW ROUTE: a URL resolved BEFORE the pause STILL WORKS after the resume (1152 ms first byte, no re-resolve). The pre-resolve design holds, and it saves the 1329 ms that resolving after a resume costs. This is the measurement the design was resting on. Q17: preview traffic DOES reset the idle clock, so layer 3 is weak and layer 2 carries the load.

**Observations.** 8 resume-to-healthy samples, against a declared minimum of 4.

Run `g3-2026-09-03T16-00-10-492Z` on plan `starter`, 2026-09-03T16:05:57.952Z.

## Resume latency (Q7)

| measurement | n | p50 | p95 | min | max |
|---|---|---|---|---|---|
| resume to healthy | 8 | 4740 ms | 5904 ms | 4413 ms | 5904 ms |
| pause API call | 8 | 12171 ms | 12516 ms | 11916 ms | 12516 ms |

## Does pause free the concurrency slot (Q1, D3)

Documented as yes: "A paused sandbox doesn't count against your plan's limit on running sandboxes; resuming it counts again" (00-verification.md V20). Observed here: **confirmed**.

created a second sandbox while the first was paused, so the paused one holds no slot

Billing itself is not observable from the client. The gate records paused durations below so they can be reconciled against the invoice, rather than claiming to have measured dollars it cannot see.

## Does the previewUrl route survive a pause and resume

| measurement | ms |
|---|---|
| fresh sandbox, first byte (route setup) | 1569 |
| same route, second request (steady state) | 1267 |
| after pause and resume, first byte | 304 |
| after pause and resume, second request | 320 |
| preview host changed across the pause | false |
| **stored URL still works after resume** | true |
| stored URL first byte after resume | 1152 |
| cost of resolving a NEW url after resume | 1329 |

**Why this matters more than it looks.** G1 measured warm-resume total at 6,124 ms, of which **2,389 ms is previewUrl first byte** and only 281 ms is the app answering on loopback. If the route established before a pause is still warm after the resume, the warm-pool replenisher touches `previewUrl` once before pausing and the visitor never pays route setup. That is roughly the difference between publishing 6.1 s and publishing something near 3.7 s.

a URL resolved BEFORE the pause STILL WORKS after the resume (1152 ms first byte, no re-resolve). The pre-resolve design holds, and it saves the 1329 ms that resolving after a resume costs. This is the measurement the design was resting on.

## Q17: does previewUrl traffic reset the idle clock

Method: one sandbox with `timeoutMs` 90000 ms and `onTimeout: "kill"`, driven with continuous HTTP through its previewUrl and **no SDK calls at all**, observed for 135000 ms.

| observation | value |
|---|---|
| timeoutMs set | 90000 ms |
| observed for | 135000 ms |
| preview requests made | 58 |
| sandbox died | false |
| time to death | n/a |
| preview traffic resets clock | true |

**What this changes.** Nothing structural. The three-layer expiry design (02-architecture.md section 8.2) deliberately does not depend on this answer. What it tells us is how much layer 3, the dead-man refresh, is actually worth: if preview traffic resets the clock, a visitor who keeps loading pages keeps the sandbox alive past a server death, and layer 2's guest-side self-destruct is the only thing that stops them.

## Cost

| | sandbox-seconds | browser-seconds | USD at starter |
|---|---|---|---|
| Estimated before start | 625 | 0 | $0.00990 |
| Actual after finish | 469.2 | 0 | $0.00734 |

### Measured versus modelled

| | USD | share |
|---|---|---|
| measured from observed lifetimes | $0.00038 | 5% |
| modelled from flat estimates | $0.00695 | 95% |

**95% of this gate's cost is modelled, not measured.** Solari exposes no balance or usage endpoint to reconcile against (remaining credit is visible only on the console Billing page), so a modelled row can drift from the real balance with nothing to catch it. D10 puts this number on a public page, so the gauge is labelled an estimate until a gate measures its own lifetimes.

| row | kind | seconds | USD | measured |
|---|---|---|---|---|
| 1 | sandbox | 30 | $0.00038 | no (modelled), concurrency probe, flat model |
| 2 | sandbox | 220 | $0.00348 | no (modelled), resume-latency sandbox, flat model |
| 3 | sandbox | 24.2 | $0.00038 | yes, route-survival probe, observed lifetime |
| 4 | sandbox | 195 | $0.00309 | no (modelled), Q17 idle-clock probe, flat model |

Ceiling for this gate was $0.03000. Total gates budget is $0.40.

Attempts recorded: 38. Any attempt beyond the first for one logical call is a retry, and the gate caps retries at zero (`CAPS_GATES`).