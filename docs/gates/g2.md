# G2: previewUrl framing, and whether the app works behind it

**Question.** What framing headers does previewUrl carry (Q3), and do absolute URLs, websockets and cookies survive behind it?

**Verdict.** Iframe refused, new-tab-plus-toolbar is the design (D5), which was already the default. Uptime Kuma websockets behind previewUrl: not determined.

**Observations.** 1 app reached behind previewUrl, against a declared minimum of 1.

Run `g2-2026-09-03T15-24-26-683Z` on plan `starter`, 2026-09-03T15:24:40.560Z.

## Framing headers

| app | reachable | X-Frame-Options | frame-ancestors | framable |
|---|---|---|---|---|
| gitea | true | SAMEORIGIN | none | false |

If no app is framable, `04-frontend-spec.md` section 3's desktop iframe layout is dead and the toolbar page is the only layout. That was already the default design, so nothing else changes.

## Does the app actually work behind previewUrl

| app | cross-host absolute URLs | cookie SameSite | websocket upgrade | tokenless request refused |
|---|---|---|---|---|
| gitea | 5: https://docs.gitea.com, https://docs.gitea.com/installation/install-from-binary | None | n/a | true |

**Cross-host absolute URLs** are the Gitea failure mode: it writes them from `ROOT_URL`, so the boot script must patch `app.ini` to the previewUrl after the fork and restart before the health check.

**Tokenless request refused** is the check T5 rests on. If a bare previewUrl without its `pt_token` is served rather than refused, the URL is not a capability and every instance needs an app-level password.

## Cost

| | sandbox-seconds | browser-seconds | USD at starter |
|---|---|---|---|
| Estimated before start | 90 | 0 | $0.00143 |
| Actual after finish | 90 | 0 | $0.00143 |

### Measured versus modelled

| | USD | share |
|---|---|---|
| measured from observed lifetimes | $0.00000 | 0% |
| modelled from flat estimates | $0.00143 | 100% |

**100% of this gate's cost is modelled, not measured.** Solari exposes no balance or usage endpoint to reconcile against (remaining credit is visible only on the console Billing page), so a modelled row can drift from the real balance with nothing to catch it. D10 puts this number on a public page, so the gauge is labelled an estimate until a gate measures its own lifetimes.

| row | kind | seconds | USD | measured |
|---|---|---|---|---|
| 1 | sandbox | 90 | $0.00143 | no (modelled), gitea framing probe, flat model |

Ceiling for this gate was $0.02000. Total gates budget is $0.40.

Attempts recorded: 3. Any attempt beyond the first for one logical call is a retry, and the gate caps retries at zero (`CAPS_GATES`).